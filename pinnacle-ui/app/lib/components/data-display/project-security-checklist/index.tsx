import { FC } from "react";
import { Project } from "@/app/lib/types/models";

const ProjectSecurityChecklist: FC<{ project: Project | undefined }> = ({
  project,
}) => {
  return <></>;
};

export default ProjectSecurityChecklist;
